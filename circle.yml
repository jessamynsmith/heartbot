machine:
  post:
    - pyenv global 2.6.8 2.7.9 3.3.3 3.4.2 3.5.0

test:
  override:
    - pip install virtualenv
    - tox

deployment:
    production:
      branch: master
      heroku:
        appname: heartbotapp
